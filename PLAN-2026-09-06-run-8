mago maintenance plan — 2026-09-06 run 8

Recon
- `gh issue list --state open` is empty — 0 open GitHub issues.
- Per AGENTS.md, the open PR queue is the current set of "open items" to triage.
- All four open AM PRs (#247, #248, #252, #253) are now `MERGEABLE`/`CLEAN` on GitHub.
- The full core+platform verify gate is green on origin/master:
    go test ./...            # ok (98.1s)
    go vet ./...             # ok
    gofmt -l .               # clean
    cd platform && go test ./... && go vet ./...  # ok (4.3s)
- #247 no longer contains the `PLAN-2026-09-06` conflict from run 7; its diff is now only test files.
- The open PRs are test/docs/plan bundles (276-490 added lines each). They exceed the am-merger strict 100-line pass-1 gate, but the bulk is tests and plan files.

Plan
1. QA: verify each open PR still passes the full gate. In a clean worktree or a `git fetch origin pull/<N>/head:pr<N>` checkout, merge origin/master and run:
      TMPDIR=/tmp/$AM_RUN_ID go test ./...
      go vet ./...
      gofmt -l .
      cd platform && TMPDIR=/tmp/$AM_RUN_ID go test ./... && go vet ./...
   Report pass/fail on the bus. Any failure is the top priority to fix before merge.
2. Merger / am-merger: once all four are green, land them via squash-merge:
      gh pr merge 247 --squash --delete-branch
      gh pr merge 248 --squash --delete-branch
      gh pr merge 252 --squash --delete-branch
      gh pr merge 253 --squash --delete-branch
   If the strict size gate blocks a bundle, apply the relaxed pass (exclude test/docs/plan files) or, if the scope is still too broad, escalate for split next run.
3. Dev: while the open PR queue is in flight, do NOT start a competing coverage PR on the files it touches. Once the queue is clear and no real `fix:` issue appears, run the coverage profiler:
      go test -coverprofile=cover.out ./...
      go tool cover -func=cover.out
      rm cover.out
   Pick the lowest-coverage, non-process-level (<70%) helper in a source file outside the claimed set and land a small `test(<scope>):` PR. The documented stop condition for process-level functions (main, serve, cmdServe, workerDoctor, etc.) still applies.
4. If a real open issue appears mid-run, drop triage/coverage work and land a focused `fix:` PR.

Boundaries
- Core stays stdlib-only; commits stay conventional.
- Respect the open PR file claims listed in the run 8 kickoff — do not edit those paths for unrelated work.
- Delete coverage artifacts before staging; never commit `cover.out` or `.new.<pid>`.
- No new third-party dependencies in the core module.
