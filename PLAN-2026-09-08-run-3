# PLAN — 2026-09-08 run 3 (architect)

Objective: "Fix open GitHub issues. Small PRs, conventional commits."

## Live queue (re-verified)

- `gh issue list --state open` → **0 open issues.** Nothing to fix.
- Open PRs: **#247, #248, #252, #253 — all `MERGEABLE` / `CLEAN`** (re-checked
  via `gh pr view --json mergeable,mergeStateStatus`).
- No stale, style-only, or bundle/duplicate PRs remain to close.
- Full verify gate green on this branch (= origin/master):
  - `gofmt -l .` → clean
  - `go vet ./...` → clean (core + platform)
  - `go test ./...` → ok (core)
  - `cd platform && go test ./...` → ok

## Triage

- #247 (test tick error branches), #248 (docs triage stop), #252 (test commands
  parse/load errors), and #253 (residual git_state/review/compact coverage) are
  all mergeable.
- These are coverage or docs PRs; no bug fixes required.
- The merger owns landing them. Dev/qa **do not** need to re-verify or rebase
  them.

## Coverage stop

- Master core coverage is ~91%; the remaining sub-75% functions are either
  process-level (`main`, `serve.run`, `cmdServe`) or already claimed by the
  open PRs (verified via coverprofile on run 2026-09-08).
- Platform residual (<70%) is daemon lifecycle, GitHub App/webhook, Stripe, and
  relay handlers — all require real credentials/network.
- Per the AGENTS.md triage stop condition, there are no unclaimed,
  non-process-level, non-credential branches left to cover.

## Plan

**Stand down.** No new dev/qa work this run.

Do **not**:
- Invent new coverage on files claimed by open PRs.
- Open duplicate or competing style/coverage PRs.
- Switch to an unrelated issue; there are none open.

This plan file is the run's commit.
