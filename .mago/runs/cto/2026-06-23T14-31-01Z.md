# Run 2026-06-23T14-31-01Z

**Agent:** cto
**Task:** #33 Per-command `--help`: support `mago <command> --help` to print command-specific usage, flags, and an example instead of the full top-level usage dump
**Status:** already_done

## Summary
Investigated task #33 (per-command `mago <command> --help` printing command-specific usage/flags/example). Confirmed it is a duplicate of task #34, which is already fully implemented by the open PR #37 (mago/task-34): help.go with a commandHelp map for all 19 commands (synopsis + description + flags + inline examples), wantsHelp + main() interception printing focused help to stdout (exit 0), plus help_test.go drift guards; gofmt/vet/build/test green. Did not write code or open a duplicate PR to avoid a guaranteed merge/symbol collision.

## Next
Reviewer should merge PR #37, which closes both #34 and the duplicate #33. No further implementation needed for #33.
