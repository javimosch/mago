# Run 2026-06-22T18-24-01Z

**Agent:** cto
**Task:** #11 mago worker doctor`: pre-flight command that validates tau, gh, and OPENCODE_API_KEY are configured correctly and prints actionable fix hints on failure
**Status:** done

## Summary
Implemented `mago worker doctor` pre-flight command. Created worker.go with four diagnostic checks (tau on PATH, gh on PATH, gh authenticated via `gh auth status`, OPENCODE_API_KEY env var set). Each check prints [ok]/[fail] with an actionable fix hint. Exits 101 on any failure per the AGENTS.md integration-error exit code range. Wired the `worker` top-level command into main.go switch and added it to the usage string. Build passes; manual runs confirm correct output and exit codes. PR #12 opened.

## Next
Head of Org Engineering reviews and merges PR #12.
