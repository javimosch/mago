# Skills index

- llm-json-prompting — LLM must be prompted to return ONLY a JSON object; stripFences utility handles c…
- state-compaction-design — Compaction must be best-effort (non-blocking) — if LLM synthesis fails, skip a…
- parse-section-order — Section parser must handle arbitrary heading order, missing sections, and empty …
- routing-tests-llm-fallback — routeTask's LLM call (tauComplete) retries 3x with 2s+4s backoff on failure. In …
- readme-troubleshooting — In this Go repo 'formatter' = gofmt (AGENTS.md requires gofmt-clean) and 'linter…
- frontmatter-helpers — parseFrontmatter/renderFrontmatter in model.go are pure functions backing all ag…
- util-helpers-tests — util.go helpers: truncate uses byte-length + byte-slicing (s[:n]) so multibyte c…
- skills-retrieval-tests — skills.go helpers are pure: tokenize keeps only >=4-rune lowercased [a-z0-9] tok…
- loop-cadence-tests — loop.go cadence is testable via 3 extracted helpers: parseLoopArgs (flags w/ def…
- github-mode-detection — loadCompany silently falls back to localBackend when ghRepo is empty; reaching g…
- command-dispatch-suggestions — main.go dispatch is a flat switch on os.Args[1]; the canonical command list now …
- worker-doctor-provider-aware — checkClaudeAuth runs a real `claude -p ping --output-format json` probe — safe…
- cli-exit-code-pattern — cmdWorker was the only cmd* function calling os.Exit directly. Use cliErr{code, …
