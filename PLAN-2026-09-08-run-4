# PLAN — 2026-09-08 run 4 (architect)

Objective: "Fix open GitHub issues. Small PRs, conventional commits."

## Live queue (re-verified)

- `gh issue list --state open` → **0 open issues.** Nothing to fix.
- Open PRs: **#247, #248, #252, #253 — all MERGEABLE / CLEAN.**
- No stale, style-only, or bundle/duplicate PRs to close.

## Dev work landed this run

- `account_link_test.go` (3 commits): covers `cmdLink` missing-token, platform
  error, POST installation error, list, no-installations, and with-installation
  branches.
- `account_auth_test.go` (1 commit): covers `cmdSubscribe` and `cmdBilling`
  missing-token branches.
- All targets are `account.go` command error branches, stdlib-only, no external
  dependencies added to core.
- Full verify gate green on current branch:
  - `gofmt -l .` → clean
  - `go vet ./...` → clean (core)
  - `go test ./...` → ok (core)
  - `cd platform && go vet ./...` → clean
  - `cd platform && go test ./...` → ok

## Plan

- **QA:** review the three `test(account):` commits and the
  `origin/master..HEAD` diff. Reject anything off-objective or not gated/verified.
- **Dev:** if QA rejects, fix and re-run the full gate; do not add more work.
- If QA passes, **no further dev work this run.**
- **Merger:** continues to own landing the open PR queue (#247-#253) and this
  run's branch.

## Coverage stop / do not

- Core coverage is now ~91%; the remaining sub-75% functions are process-level
  (`main`, `serve.run`, `cmdServe`) or already claimed by the open PRs.
- Do not invent new coverage on files claimed by open PRs.
- Do not open duplicate or competing style/coverage PRs.

This plan file is the architect's run-4 commit.
