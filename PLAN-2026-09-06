mago maintenance plan — 2026-09-06

Recon
- javimosch/mago has 0 open GitHub issues.
- One open AM PR: #244 (test/account: cover readCreds --email override, plus cmdMode and
  selectSkillsText branches, and a 2-line AGENTS.md coverage-note). It is MERGEABLE/CLEAN.

Plan
1. QA: verify PR #244 with the full core+platform gate and a private TMPDIR:
     TMPDIR=/tmp/<run> go test ./... ; go vet ./... ; gofmt -l .
     cd platform && go test ./... && go vet ./...
   If green, squash-merge with a title that reflects all three test scopes.
2. Dev: once #244 is merged, pick the next unclaimed low-coverage helpers, one per small PR:
     - relay.go — workerID env/hostname fallback
     - debri.go — writeDebriPromptFile success/cleanup path
     - loop.go — cmdLoop / parseLoopArgs error branches
   Each must be stdlib-only, a conventional `test(<scope>):` commit, and pass the full gate.
3. If a real open issue appears, drop coverage work and land a focused `fix:` PR.
4. Until then, treat the open PR queue as the "open issues" list and keep triage first.

Run 3 update (architect, 2026-09-06 ~12:30Z)

Recon
- Still 0 open GitHub issues; all historical issues are CLOSED.
- Open PR queue: #247 (tick/cmdRun error branches), #248 (AGENTS.md triage note +
  debri/relay/route edge tests), #249 (cmdRegister/state_compact/tau branches). All three
  are AM-authored and MERGEABLE — nothing stale, duplicate, or style-only to close.
  The merger owns landing them; triage yields no close actions.
- Master gate is green; statement coverage 90.5%. Functions still <70% are process-level
  (main 26%, serve run 27%, cmdServe 44%) — the stop condition #248 documents.
- Residual unclaimed wins DO remain: uncovered `cmd*` wrapper error branches (bad `-C`,
  missing `.mago`) and a few pure error paths in files NOT claimed by #247–#249.

Plan
1. Dev: ONE small PR, unclaimed files only:
     - loop_test.go — cmdLoop parseCompanyDir + loadCompany error branches
       (`-C` dangling flag; `-C <empty tempdir>`), agent=="" reconcileOnce path if cheap.
     - commands_test.go — cmdTask/cmdProject/cmdStatus/cmdAnswer loadCompany-error and
       parseCompanyDir-error branches via `-C <empty tempdir>` / dangling `-C`.
     - backlog_error_test.go — plannerAgent loadAgentNames error -> nil, setMission with
       missing STATE.md -> no-op return.
     - worker_daemon_test.go — daemonizeWorker "worker already running" branch via a
       pidfile containing os.Getpid().
   Skip anything needing process spawn, /proc, or network — that is the documented stop
   condition. Do NOT touch files claimed by #247/#248/#249.
2. QA: full core+platform gate with a private TMPDIR (see step 1 above for the recipe).
3. If a real open issue appears mid-run, drop coverage work and land a focused `fix:` PR.
