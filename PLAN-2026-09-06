mago maintenance plan — 2026-09-06 (triage)

Recon
- javimosch/mago has 0 open GitHub issues.
- The only open AM PR was #246 ([needs work] test(account): cover readCreds
  interactive prompt paths). It bundled a PLAN-2026-09-06 update with three
  legitimate, already-green test-coverage commits and conflicted with master
  because the plan file was already added by #245.
- Full core+platform verify gate is green on this branch:
  core 90.5% coverage, go test ./..., go vet ./..., gofmt -l . and
  cd platform && go test ./... && go vet ./... all pass when run with a
  private TMPDIR.

Triage (done)
1. Closed PR #246 as a stale bundle/duplicate. The plan file it added was
   already on master; the three test commits it contained are good and should
   be resubmitted as focused, small PRs.

Next (dev)
2. Re-land the three closed PR #246 test commits on a clean branch, one
   conventional `test(<scope>):` commit per concern:
   - test(account): cover readCreds interactive prompt paths
     (account_readCreds_test.go; feed one line per prompt to avoid bufio
     read-ahead starving the next prompt)
   - test(backend): cover PickActiveTask ListTasks error passthrough
     (backend_test.go; regular file at tasks path => ENOTDIR surfaced)
   - test(selfupdate): cover downloadFile unwritable-destination branch
     (selfupdate_test.go; 200 response with missing parent dir)
3. QA runs the full core+platform verify gate with a private TMPDIR and
   deletes cover.out / cover.html before any git add.

After the queue is clear
4. Dev picks the next unclaimed, low-coverage core helper for a small
   stdlib-only error-branch test, one file per PR:
   - relay.go — workerID env/hostname fallback (empty MAGO_WORKER_ID,
     os.Hostname error, default "worker")
   - debri.go — writeDebriPromptFile success/cleanup and WriteString error
     paths
   - loop.go — cmdLoop / parseLoopArgs error branches (bad company dir,
     missing --base/--max value, loadCompany errors)
5. QA runs the full gate.
6. If a real open issue appears, drop coverage work and land a focused `fix:`
   PR.

Invariants
- Core stays stdlib-only; no third-party dependencies in the root module.
- Use a private TMPDIR for test runs (e.g. TMPDIR=/tmp/<run> go test ./...)
  when sibling agents share the worktree.
- Delete cover.out / cover.html before staging; never commit them.
- Do not invent new issues when the issue list is empty.
