# PLAN — 2026-09-08 run 5 (architect)

Objective: "Fix open GitHub issues. Small PRs, conventional commits."

## Live queue (re-verified)

- `gh issue list --state open` -> 0 open issues. Nothing to fix directly.
- Open PRs: **#247, #248, #252, #253** — all `MERGEABLE` / `CLEAN`.
- `gofmt -l .` and `cd platform && gofmt -l .` are clean.
- No stale style-only PRs, no obvious bundle/duplicates to close.

## Plan

- **No new code this run.** The open PR queue is the only work.
- **am-merger** to land #247, #248, #252, #253 next:
  - All are test/doc/generated-only; safe under the AM pass-2 relaxed gate (effective line count excludes tests/docs/generated after a pass-1 line-count fail).
  - All have safe PR types (`test:`, `docs:`, `chore:`), no veto labels, are not drafts, are ≥ 30min old, and are `MERGEABLE`.
- Once #247-#253 are landed, the open PR queue is clear and the coverage stop can be re-evaluated.
- **dev/qa stand down** until the queue is clear. Do not start new coverage PRs.

## Coverage stop / do not

- Core coverage is ~91%; the remaining sub-75% functions are process-level (`main`, `serve.run`, `cmdServe`) or already claimed by the open PRs.
- Do not invent new coverage on files claimed by open PRs.
- Do not open duplicate or competing style/coverage PRs.
