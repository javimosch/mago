# PLAN — 2026-09-08 (architect)

Objective: "Fix open GitHub issues. Small PRs, conventional commits."

## Queue state (verified live)

- `gh issue list --state open` → **0 open issues.** Nothing to fix.
- Open PRs: **#247, #248, #252, #253 — all CLEAN/MERGEABLE.** The run-7 rebase
  resolved #247's PLAN-file conflict. No stale, style-only, or bundle/duplicate
  PRs remain to close. Landing them is the merger's job, not dev's.
- Master: `gofmt -l .` clean; core `go test ./...` green at **91.0%** coverage;
  platform green at 60.4%.

## Coverage residual (measured, not assumed)

Core <75% funcs are all either process-level or already claimed by open PRs:

| func | cov | disposition |
|---|---|---|
| main / serve.run / cmdServe | 27–46% | process-level → **stop** (AGENTS.md triage rule, documented by #248) |
| tick.runTick, selfupdate.selfUpdate | 60–68% | claimed by #247 |
| debri.writeDebriPromptFile, route.reconcileOnce | 70–71% | claimed by #248 |
| git_state.pushDefs | 74% | claimed by #253 |
| platform store.openStore / Update | 67–73% | claimed by #253 (platform/store_edges_test.go) |

Platform's remaining 0% funcs are daemon lifecycle, GitHub App/webhook, Stripe,
and relay handlers — all need real credentials/network → **stop**.

## Plan for dev/qa

**Stand down — no new work this run.** Every uncovered path is either
process-level or inside a file claimed by an open AM PR. Per AGENTS.md:
"Only after the open queue is clear should dev pick small, unclaimed
test-coverage wins" — the queue is not clear, and there are no unclaimed
non-process-level branches left anyway. Do not invent coverage on claimed
files and do not open competing style/duplicate PRs.

This plan commit itself satisfies the run's commit requirement.
